{
  "name": "fintrack",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web": "bun run --cwd=apps/webapp dev",
    "api": "bun run --cwd=apps/api dev",
    "gateway": "bun run --cwd=services/gateway dev"
  }
}